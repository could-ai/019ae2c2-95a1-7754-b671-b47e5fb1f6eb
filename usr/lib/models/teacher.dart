// Teacher model for teacher information
class Teacher {
  String name;
  String subject;
  String phone;
  String? photoUrl; // Optional photo

  Teacher(this.name, this.subject, this.phone, this.photoUrl);
}